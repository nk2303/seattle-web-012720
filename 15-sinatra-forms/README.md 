
## Hi
- To open a second tab in your terminal press command and t 
- debugging 


# Sinatra RESTful Routes

| CRUD Letter | RESTful Route           | Controller Action |
|-------------|-------------------------|-------------------|
| C(reate)    | POST /dishes            | create            |
| R(ead)      | GET /dishes             | index             |
| R(ead)      | GET  /dishes/:id        | show              |
| U(pdate)    | PATCH /dishes/:id       | update            |
| D(elete)    | DELETE /dishes/:id      | destroy           |


## SWBATs:
    [ ] implement destroy with postman
	[ ] explain MVC pattern with valid examples 
	[ ] implement proper MVC file structure
	[ ] build ERB Template with a form
	[ ] demonstrate the connection between form inputs and params 
	[ ] implement create with a form
	[ ] implement edit and update
	[ ] describe RESTful conventions 
	[ ] explain the connection between Restful Routes and CRUD



## Deliverables
    You’ve taken a role as Flatirons new Lunch person. 
    Your first task is to build a menu application that can be viewed in the browser 
    using the existing database of pre-approved meals. 
    You’d like to keep the menu up to date so you make a note to add functionality that will update 
    existing menu items and add new ones.

   ### Users should be able to....
   - [x] view all menu items at once
   - [x] view information about a specific item using its ID
   - [x] delete existing menu items (with postman)
   - [x] create new menu items (with form)
   - [x] update existing menu items (with form)
